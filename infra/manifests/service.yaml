apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: dep-graphql-pyp
  name: dep-graphql-pyp
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8000
  selector:
    app: dep-graphql-pyp
status:
  loadBalancer: {}
